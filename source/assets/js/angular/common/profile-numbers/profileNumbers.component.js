import template from './profileNumbers.html';
import controller from './profileNumbers.controller';

let profileNumbersComponent = {
  restrict: 'E',
  bindings: {},
  template: template,
  controller: controller
};

export default profileNumbersComponent;
