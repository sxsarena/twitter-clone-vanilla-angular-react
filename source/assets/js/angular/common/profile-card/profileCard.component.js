import template from './profileCard.html';
import controller from './profileCard.controller';

let profileCardComponent = {
  restrict: 'E',
  bindings: {},
  template: template,
  controller: controller
};

export default profileCardComponent;
